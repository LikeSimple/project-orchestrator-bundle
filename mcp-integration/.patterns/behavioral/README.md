# 行为型模式

本目录包含 7 种行为型设计模式。

## 模式列表

| 模式 | 说明 |
|------|------|
| [Observer (观察者)](./observer.md) | 定义对象间的一种一对多的依赖关系，当一个对象的状态发生改变时，所有依赖于它的对象都得到通知并被自动更... |
| [Strategy (策略)](./strategy.md) | 定义一系列的算法，把它们一个个封装起来，并且使它们可相互替换。本模式使得算法可独立于使用它的客户而变... |
| [Command (命令)](./command.md) | 将一个请求封装为一个对象，从而使你可用不同的请求对客户进行参数化；对请求排队或记录请求日志，以及支持... |
| [Template Method (模板方法)](./templateMethod.md) | 定义一个操作中的算法的骨架，而将一些步骤延迟到子类中。模板方法使得子类可以不改变一个算法的结构即可重... |
| [State (状态)](./state.md) | 允许一个对象在其内部状态改变时改变它的行为。对象看起来似乎修改了它的类。... |
| [Iterator (迭代器)](./iterator.md) | 提供一种方法顺序访问一个聚合对象中各个元素，而又不需暴露该对象的内部表示。... |
| [Chain of Responsibility (责任链)](./chainOfResponsibility.md) | 使多个对象都有机会处理请求，从而避免请求的发送者和接收者之间的耦合关系。将这些对象连成一条链，并沿着... |

## 使用说明

每个模式文件包含：
- 模式的详细说明
- 适用场景
- 优缺点分析
- 多框架代码示例（TypeScript、React、Vue 3、Node.js）
